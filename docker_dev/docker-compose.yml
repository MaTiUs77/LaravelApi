version: "3"
services:
    siep-api-web:
      restart: always
      image: nginx:alpine
      container_name: siep-api-web
      working_dir: /application
      volumes:
          - ..:/application
          - ./phpdocker/nginx/nginx.conf:/etc/nginx/conf.d/default.conf
      ports:
       - "3000:80"
      depends_on:
          - siep-api-php

    siep-api-php:
      restart: always
      build: phpdocker/php-fpm
      container_name: siep-api-php
      working_dir: /application
      volumes:
        - ..:/application
        - ./phpdocker/php-fpm/php-ini-overrides.ini:/etc/php/7.0/fpm/conf.d/99-overrides.ini
      environment:
        - DB_HOST=172.17.0.1
        - MEMCACHED_HOST=siep-api-memcached
        - API_HOST_REMOTO=http://172.17.0.1:3000

    siep-api-memcached:
      restart: always
      image: memcached:alpine
      container_name: siep-api-memcached
